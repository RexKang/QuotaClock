# 更新日志

本项目的所有重要变更都记在这里。版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)；格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

GitHub Release 的正文由 `scripts/release_notes.py` 从本文件 + git 提交自动拼装（详见 README「发布流程」）。

## [v0.2.5] - 2026-09-12

### 新增

- **平台预设化**：只支持 GLM / DeepSeek / Kimi Code / OpenCode 四个内置平台，不允许自建。用户只填 API Key（或 Cookie 内容），接口地址、请求路径、鉴权方式由程序内置——各家的返回结构需要配套解析逻辑，自填地址会「接口通了但字段认不出来」。
- **同平台多 Key**：一个平台可挂 N 个 Key，采集周期内按 `i × (周期 ÷ N)` 等分错开，同平台请求串行化，避免互相冲突；退避分两层——平台层（5xx / 网络）停该平台全部 Key，Key 层（429）只停该 Key。
- **采集状态三态语义**：卡片徽标区分「等待采集」（新加的 Key、服务端刚启动首轮未回）、「采集异常」（429 限流等可自愈情况，黄色 + 重试倒计时）、「采集失败」（5xx / 超时 / 网络 / 业务失败，红色 + 重试倒计时）。
- **设置面板三标签**：平台管理 / 采集与服务（采集间隔、监听 IP 与端口）/ 账户安全。
- **未保存也能测 Key**：新增的 API Key 先点「测试」确认连通，再保存；测试用凭据只在本次请求内存里，不落盘不进日志。
- **配置迁移前自动整份备份**：schema 升版时先写 `<配置>.v<旧版本>.bak`（原文件字节级相同），备份失败则中止迁移、原文件不动。

### 变更

- 配置 schema 由 version 3 升到 **version 4**：`provider` 只存 `platform` + `access_keys[]`，`base_url`/`paths`/`auth_style`/`extra_headers` 不再落盘也不出现在界面；v0.2.x 配置按 `base_url` 自动迁移（同平台多条合并为一平台多凭据），识别不出的条目跳过并 WARN。
- 时间戳改按服务端**本地时区**输出（RFC3339 带 `+08:00`），不再强制 UTC。
- 页头按钮顺序固定为「刷新 / 设置 / 登出」（未登录时第三位为「登录」）；账户安全页不再重复放登录/登出按钮。
- 平台管理界面收敛：去掉接口地址与请求路径展示；新增的 Key 必填（前后端双门）；清除 Cookie 相关字样（四个平台统一 Bearer）；平台全部添加后下拉显示「选择平台」并置灰按钮。
- Kimi Code 的 `/me`、`/models` 不再采集（结果页面从未渲染，却让每个 Key 每轮多发 2 个请求）。

### 修复

- 只改 API Key 的名称（或保存一份没有实质改动的配置）不再触发重新采集：名称移出「定义变更」判定；Key 级成功按等分周期排下一次可采时刻，保存配置唤醒的额外 tick 会跳过未到期的 Key。
- 带请求体的请求被提前拒绝（未登录 401 / 登录 400 / 限速 429）时不再强关连接，消除客户端偶发丢响应（约 3% 概率）。
- 缓存恢复不再改写时间戳时区（重启前后同一次成功采集显示同一时间）。
- 已停用的凭据优先显示「已停用」，不再被「token 无法解密」盖成「Key 失效」。

## [v0.2.4] - 2026-09-12

### 新增

- 平台/凭据级**启用开关**：停用的凭据不参与采集，首页与详情页都不展示，设置列表里显示「停用」标记。
- **`cache.json` 上次成功数据缓存**：服务端重启后首屏立刻显示上次数据（徽标「缓存数据」），首轮采集成功后转「正常」，不再回到空白等待。
- 设置面板拆「平台管理 / 账户安全」双标签。

### 变更

- v0.1 迁移语义：`enabled=false` 的平台由「跳过」改为「导入并保持停用」。
- **降级提示**：v0.2.4 之前的二进制读含 `enabled` 字段的配置会因未知字段拒绝启动，回退旧版本前需手动删除该字段。

### 修复

- 停用平台不再占首页卡片，改为状态行提示。
- 平台列表去掉恒显的 token ✓，改为缺 token 时告警。
- 详情页也只展示在用平台。

## [v0.2.3] - 2026-09-10

### 新增

- OpenCode 切换**官方用量接口**（Bearer API Key）：`GET /zen/go/v1/usage`。v0.1 的 Cookie 登录态无法转换为 API Key，迁移时该 token 不带过来，需到控制台生成 API Key 后录入。

### 文档

- README 版本徽章与构建示例更新至 v0.2.3。

## [v0.2.2] - 2026-09-07

### 变更

- 未登录时隐藏「设置」入口（admin 模式下不再暴露可点但打不开的按钮）。

## [v0.2.1] - 2026-09-07

### 修复

- 空配置下 `providers` 被序列化成 `null`，导致新环境首次添加平台时前端崩溃。

## [v0.2.0] - 2026-09-06

### 新增

- **Go 单二进制服务端化**：把 v0.1 的「单页 + Python 代理」换成静态页与 API 同进程提供，配置从浏览器 `localStorage` 搬到服务端**加密落盘**（AES-256-GCM，密钥为随机生成的 `key.bin`），支持 Windows / Linux（amd64、arm64）。

### 变更

- 浏览器侧不再直连各家平台：请求改由服务端发起（同源 `/api/*`），因此**不再需要 Python 反向代理**、不再受 CORS 限制。
- 配置入口从浏览器存储变为服务端 `config.json`：v0.1 导出的 JSON 可通过旁路文件迁移（见 README「从旧版本迁移」）。

### 修复

- Linux 探活误把 ticks 与秒比较，导致自探活恒判死（CI 在 ubuntu 上暴露）。

## [v0.1.0] - 2026-09-05

### 新增

- 首个公开版本：多平台用量看板（智谱 GLM / DeepSeek / Kimi / OpenCode），双进度条对比用量与时间基准、卡片可折叠、平台级状态标语、额度耗尽提示与一键切换健康平台。
- 形态：**单页 HTML（`index.html`）+ Python 标准库写的反向代理（`llm-proxy.py`，`127.0.0.1:8787`，转发并注入 CORS 头，解决 opencode.ai / api.kimi.com 不放行跨域的问题）**。
- 配置只存浏览器 `localStorage`（刷新不丢），并可「导出 JSON」做备份；平台地址在本机代理与直连之间自行填写。
